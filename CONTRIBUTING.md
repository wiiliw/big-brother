# 维护指南

## 更新资料

1. 先查 `docs/catalog.json` 和 `docs/FACT_CHECKS.md`，确定本次核验范围。每次改动集中处理一个主题。
2. 优先读取监管部门原文、交易所公告和上市公司定期报告。在事实旁记录可访问的来源链接、事件日期、数据截止日期；转载不能代替原始证据。
3. 名单变更记录证券代码、上市状态、实际控制人和证据日期；参股、控股和拟入主分开描述。合并、退市或改名保留变更原因，不能仅根据简称匹配。
4. 更新台账。`as_of` 为内容覆盖日期（YYYY-MM-DD；原稿只给月份则保留 YYYY-MM），不是编辑日期。`last_verified` 仅在全文核验后填写，否则保持 null。`pending` 表示未核验，`partial` 表示局部核验，`verified` 表示本文件本次范围全部核验。`scope` 写清范围与缺口，`review_days` 为建议复核周期。
5. 新增根目录研究 Markdown 必须加入台账并从 README 链接。具体事实纠错记入核验记录，保留原说法、纠正结果与证据。
6. 执行 README 中的检查和测试，提交可解释的差异。

## 检查与复核

每周 Actions 生成离线报告；它不自动抓取行情、不更新文章，也不证明事实正确。普通 CI 对结构错误失败，对历史资料待复核发出警告；正式声明“全部核验”前运行 `python3 scripts/check.py --strict`。月度复核名单，政策变更时复核对应政策段落。

月度日期按当月1日计算逾期，宁可提前提示。脚本检查常见内联 Markdown 文件链接，不验证外部链接存活、引用式链接或标题锚点。涉及这些链接时人工打开确认。

## Skill 维护

保留分析视角，但事实优先于角色修辞。不要用“当前”覆盖旧案例的日期，不从股价或ETF份额反推确定的监管意图。安装时复制整个仓库资料，避免 Skill 引用缺失。
